name: Ping Streamlit App

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app to keep it awake
        run: curl -s -o /dev/null -w "%{http_code}" https://milklab-noppanun.streamlit.app/#groove-and-gear
